---
title: "Revoke API token"
description: "Revoke an API token by id."
---

```http
DELETE /api/v1/auth/tokens/{id}
```

Revoke a token immediately. Subsequent requests with it return `401`.

**Path parameters**

| Param | Type |
|---|---|
| `id` | string |
