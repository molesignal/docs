---
title: "Create pipeline"
description: "Attach functions to a stream as a pipeline."
---

```http
POST /api/v1/pipelines
```

Create a pipeline (unique on `stream` + `enabled`) that applies one or more functions on the ingest
hot path.
