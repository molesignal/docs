---
title: "Get a stream"
description: "Fetch one stream by id."
---

```http
GET /api/v1/streams/{id}
```

Returns the stream definition, schema, explicit `retention`, calculated `effective_retention`, and
stream settings. It requires `streams.read`; system-scope telemetry access may use
`sys.telemetry.read`.
