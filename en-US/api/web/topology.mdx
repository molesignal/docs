---
title: "Service topology"
description: "Nodes and edges from the service graph."
---

```http
GET /api/v1/web/topology
```

Aggregates `service_graph_edges` into nodes and edges within the `from`/`to` window (RFC3339).
