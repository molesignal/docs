---
title: "Alerting and incident response"
description: "Rules, incidents, anomaly detection, templates, schedules, escalations, silences, subscriptions, and insights."
---

Open **Alerts** to move from detection to acknowledgement, escalation, and resolution.

## Alert center

| Section | What you do |
|---|---|
| **Incidents** | Triage active incidents, inspect context, acknowledge, resolve, silence, or start RCA. |
| **Rules** | Create scheduled, real-time, or anomaly rules and severity thresholds. |
| **Channels** | Configure email, Slack, and secure webhook delivery. |
| **Templates** | Reuse notification subjects, bodies, and variables; send test messages. |
| **History** | Review past incident and rule activity. |
| **Insights** | Inspect volume, resolution time, fast-resolution ratio, and activity by hour. |
| **Escalations** | Define ordered targets and acknowledgement timeouts. |
| **Schedules** | Build timezone-aware rotations and temporary overrides. |
| **Silences** | Suppress matching notifications for a controlled time window. |
| **Subscriptions** | Add notifications by label matchers and minimum severity. |
| **Semantic groups** | Normalize labels into reusable incident-grouping semantics. |

## Rule types

<CardGroup cols={3}>
  <Card title="Scheduled" icon="clock">
    Runs a SQL or metric query on an interval and evaluates its trigger and severity thresholds.
  </Card>
  <Card title="Real-time" icon="bolt">
    Records `real_time` rule intent. The current backend evaluates it through the scheduled
    evaluator; an ingest-path matcher is not active yet.
  </Card>
  <Card title="Anomaly" icon="wave-square">
    Compares current values with a historical MAD or EWMA baseline, optionally with weekly
    seasonality.
  </Card>
</CardGroup>

Use the rule workbench to select the stream, query, evaluation range, thresholds, labels,
annotations, template, and escalation policy. Test the query before enabling the rule.

Anomaly rules accept a 1–30 day lookback, a positive deviation multiplier, and either `mad` or
`ewma`. EWMA also uses an alpha value in the range `(0, 1]`.

## Incident lifecycle

An incident starts active. A responder can acknowledge it, silence matching delivery, and resolve
it after recovery. Incident detail preserves rule context, trace ids, labels, delivery state, and
related RCA when available.

Semantic grouping combines incidents with the same normalized fingerprint. Acknowledging or
resolving a group cascades to its member incidents.

Mole Agent RCA requires the `intelligence` feature and Intelligence permissions. It stores the
evidence-backed result with the incident instead of replacing the incident lifecycle.

## Escalation and on-call

The `alert_manager` role evaluates rules and drives escalation:

1. resolve the active step's users, teams, schedules, and channels;
2. dispatch each target and record its independent delivery result;
3. wait for acknowledgement until `ack_timeout_secs`;
4. advance to the next step until acknowledged, resolved, or exhausted.

Schedules use an explicit timezone, rotations, and date-bounded overrides. Use the on-call preview
before assigning a schedule to production escalation.

## Channels and templates

Email uses the deployment SMTP settings. Slack and generic webhooks are configured as organization
resources. Webhook secrets and sensitive headers are never returned in plaintext.

Templates can reference incident, rule, label, and organization fields. Use **Send test** before
binding a template to an active rule.

## Silences and subscriptions

A silence matches labels during a fixed window and pauses delivery without changing the rule.
Silencing one active incident can create a matching silence rule.

A subscription adds channel or user recipients when its label matchers and minimum severity match.
It supplements the escalation targets; it does not replace them.

## Permissions

| Permission | Capability |
|---|---|
| `alerts.read` | Read rules, incidents, templates, insights, groups, subscriptions, and silences. |
| `alerts.manage` | Create and change rules, channels, templates, escalations, groups, and subscriptions. |
| `alerts.acknowledge` | Acknowledge and resolve incidents or incident groups. |
| `alerts.silence` | Create, change, and remove silences. |
| `schedules.read` | Read schedules and on-call results. |
| `schedules.manage` | Create schedules, rotations, and overrides. |

<Card title="Webhook channels" icon="webhook" href="/en-US/alert-webhook-channels">
  Configure authentication, signatures, retries, and transport security for alert webhooks.
</Card>
