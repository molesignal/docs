---
title: "服务图边"
description: "原始服务图边。"
---

```http
GET /api/v1/traces/service_graph
```

**参数：** 必填的 `from`、`to` Unix 微秒，以及可选 `service`。响应包含 `edges[]` 与
`count`。

需要 `streams.query`，系统范围可使用 `sys.telemetry.read`。
