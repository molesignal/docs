---
title: "Elasticsearch _bulk"
description: "直接替代 Elasticsearch _bulk 接入。"
---

```http
POST /api/v1/_bulk
```

Elasticsearch `_bulk` 兼容接入（NDJSON）。适配 Filebeat、Logstash、Vector ES sink。返回逐记录状态
数组（ES 兼容的 `items[]`）。
