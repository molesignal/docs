---
title: "执行查询"
description: "在日志、指标、追踪上运行 SQL 或 PromQL。"
---

```http
POST /api/v1/query
```

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `org_id` | string | ✅ | 租户 id（登录获得）。 |
| `language` | string | ✅ | `sql` 或 `promql`。 |
| `statement` | string | ✅ | 查询文本。 |
| `time_range` | object | ✅ | `{ "start": <微秒>, "end": <微秒> }`。 |
| `stream` | object | — | `{ "name", "stream_type" }`；`stream_type` ∈ `logs`/`metrics`/`traces`/`enrichment`。 |
| `limit` | integer | — | 行数上限。 |

发送 `Accept: application/x-ndjson` 可流式返回（绕过 `query_result` 缓存）。

<RequestExample>
```bash cURL
curl -X POST http://localhost:5080/api/v1/query \
  -H "authorization: Bearer $MS_JWT" \
  -H 'content-type: application/json' \
  -d "{\"org_id\":\"$MS_ORG\",\"language\":\"sql\",
       \"statement\":\"SELECT level, count(*) FROM app GROUP BY level\",
       \"time_range\":{\"start\":0,\"end\":2000000000000000},
       \"stream\":{\"name\":\"app\",\"stream_type\":\"logs\"}}"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "rows": [{ "level": "error", "count": 12 }],
  "columns": ["level", "count"],
  "scanned_rows": 10432,
  "took_ms": 18,
  "cache_hit": false
}
```
</ResponseExample>
