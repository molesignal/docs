---
title: "简介"
description: "日志、指标、追踪同处一地 —— 自托管、OpenTelemetry 原生，没有 Datadog 的账单。"
---

**MoleSignal** 是一个自托管、OpenTelemetry 原生的可观测性平台。日志、指标、追踪共享**同一存储层**
（对象存储上的 Parquet）、**同一查询引擎**（DataFusion + Arrow）和**同一元数据层**（Postgres）——
因此同一分钟里的一条追踪、它的日志、以及主机指标是*真正*连在一起的，而不是靠人工拼接。

<Note>
  MoleSignal 处于 1.0 之前、持续开发中。会有粗糙之处，欢迎
  [提 issue](https://github.com/molesignal/molesignal/issues) —— 每一份反馈都会塑造下一个版本。
</Note>

## 为什么选 MoleSignal

今天的遥测工具强加一个糟糕的取舍：

- **商业 SaaS**（Datadog、New Relic、Splunk）能关联三类信号，但账单随流量线性增长 —— 中型团队
  100 GB/天 很容易就要付 **每月 2000–10000 美元**。
- **开源栈**（Loki + Mimir + Tempo，或 ELK + Prometheus + Jaeger）免费，但日志、指标、追踪分散在
  **三个存储、三种查询语言**里。故障时人人都要做的 "追踪 ↔ 日志 ↔ 主机指标" 跳转，只能靠手工拼接。

MoleSignal 走第三条路：三类信号在**数据平面**就被关联，而不是在仪表盘平面。自托管，所以账单
就是 S3 的成本。

|  | 商业 SaaS | 开源栈 | **MoleSignal** |
|---|---|---|---|
| 100 GB/天 成本 | ~2000+ 美元/月 | 仅基础设施 | **仅基础设施** |
| 三类信号同存储 | ✅（其云端） | ❌ 三套存储 | **✅ Parquet + DataFusion** |
| 跨信号关联 | ✅（付费） | ⚠️ 手工复制粘贴 | **✅ 原生** |
| 数据所有权 | 在其云端 | 自托管 | **自托管** |
| 搭建耗时 | 5 分钟 | 6 小时以上 | **一条命令 `docker compose up`** |
| 实时告警（&lt;1 秒） | 有 | 无 | **有（`kind: realtime`）** |

## 开始使用

<CardGroup cols={2}>
  <Card title="快速开始" icon="rocket" href="/zh-Hans/quickstart">
    几分钟内拉起沙箱、发送第一条遥测数据，并跑一个跨信号查询。
  </Card>
  <Card title="核心概念" icon="diagram-project" href="/zh-Hans/concepts">
    组织、流、信号，以及关联在底层是怎么工作的。
  </Card>
  <Card title="数据接入" icon="inbox" href="/zh-Hans/ingestion">
    10 种即插即用接入协议：OTLP、Prometheus、Loki、Elasticsearch 等。
  </Card>
  <Card title="查询" icon="magnifying-glass" href="/zh-Hans/query">
    一个引擎上的完整 SQL 与 PromQL 子集，横跨三类信号。
  </Card>
</CardGroup>

## 核心能力

- **跨信号关联** —— 在 `指标 → 追踪 → 日志 → 主机` 间来回下钻，不丢失上下文。
- **10 种接入协议** —— OTLP、Prometheus remote_write、Loki、Elasticsearch `_bulk`、Syslog 等的直接替代。
- **统一查询引擎** —— DataFusion SQL（join/CTE/窗口函数）外加 PromQL 子集。
- **告警** —— `scheduled`、`realtime`（&lt;1 秒触发）、`anomaly` 三类规则，配多级升级策略。
- **多租户与安全** —— planner 级组织隔离、API token、审计日志、字段级加密。
- **运维** —— 6 个无状态角色、单一二进制、Kubernetes 清单、Prometheus `/metrics`。
