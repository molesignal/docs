---
title: "查询"
description: "运行 SQL 或 PromQL、NDJSON 流式返回、拉取事件上下文、运行保存的视图。"
---

单一端点在三类信号上同时服务 SQL 与 PromQL。指南见 [查询](/zh/query)。

## 执行查询

```http
POST /api/v1/query
```

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `org_id` | string | ✅ | 租户 id（登录获得）。 |
| `language` | string | ✅ | `sql` 或 `promql`。 |
| `statement` | string | ✅ | 查询文本。 |
| `time_range` | object | ✅ | `{ "start": <微秒>, "end": <微秒> }`。 |
| `stream` | object | — | `{ "name", "stream_type" }`；`stream_type` ∈ `logs`/`metrics`/`traces`/`enrichment`。 |
| `limit` | integer | — | 行数上限。 |

发送 `Accept: application/x-ndjson` 可流式返回（绕过 `query_result` 缓存）。

<RequestExample>
```bash cURL
curl -X POST http://localhost:5080/api/v1/query \
  -H "authorization: Bearer $MS_JWT" \
  -H 'content-type: application/json' \
  -d "{\"org_id\":\"$MS_ORG\",\"language\":\"sql\",
       \"statement\":\"SELECT level, count(*) FROM app GROUP BY level\",
       \"time_range\":{\"start\":0,\"end\":2000000000000000},
       \"stream\":{\"name\":\"app\",\"stream_type\":\"logs\"}}"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "rows": [{ "level": "error", "count": 12 }],
  "columns": ["level", "count"],
  "scanned_rows": 10432,
  "took_ms": 18,
  "cache_hit": false
}
```
</ResponseExample>

## 流式查询

```http
GET  /api/v1/query/stream     # 参数：sql, stream, stream_type, from, to, limit
POST /api/v1/query/stream     # 与 POST /query 同结构
```

始终返回 NDJSON 分块响应。请在入口关闭代理缓冲（`proxy_buffering off`）以便分块及时下发。

## 事件上下文（search around）

```http
POST /api/v1/query/search_around
```

返回某条事件前后各 N 条（日志上下文视图）。

**请求体**

| 字段 | 类型 | 必填 | 默认 |
|---|---|---|---|
| `event_timestamp_us` | integer | ✅ | — |
| `stream` | string | ✅ | — |
| `stream_type` | string | ✅ | — |
| `event_fingerprint` | string | — | — |
| `before` | integer | — | 50 |
| `after` | integer | — | 50 |

<ResponseExample>
```json 200
{
  "before": [{ "_timestamp": 1700000000000000, "msg": "..." }],
  "after":  [{ "_timestamp": 1700000000500000, "msg": "..." }]
}
```
</ResponseExample>

## 保存的视图与仪表盘

```http
GET  /api/v1/saved_views          # ?pinned=true
POST /api/v1/saved_views
POST /api/v1/saved_views/{id}/run # 运行保存的查询
POST /api/v1/dashboards           # 创建（原生或 Grafana 导入）
POST /api/v1/folders              # 创建文件夹
```
