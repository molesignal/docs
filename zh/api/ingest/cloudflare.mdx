---
title: "Cloudflare Logpush"
description: "Cloudflare Logpush 目标。"
---

```http
POST /api/v1/_cloudflare
```

Cloudflare Logpush push（NDJSON）。成功返回 `204`。

<Note>
  用 `X-Connector-Token` 头认证，而非 bearer token。
</Note>
