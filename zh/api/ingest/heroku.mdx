---
title: "Heroku log drain"
description: "Heroku log drain 目标。"
---

```http
POST /api/v1/_heroku
```

Heroku log drain push（text）。成功返回 `204`。

<Note>
  用 `X-Connector-Token` 头认证，而非 bearer token。
</Note>
