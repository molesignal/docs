---
title: "关联与 Web"
description: "跨信号关联、服务拓扑、追踪树、命令面板搜索、调查栈。"
---

这些端点驱动 Web shell 的跨信号工作流。指南见 [跨信号关联](/zh/correlation)。

## 跨信号关联

```http
GET /api/v1/web/correlation/{from_kind}/{to_kind}
```

为 `metric ↔ trace ↔ log ↔ host` 跳转做服务端的过滤条件与时间范围预填。

**参数**

| 参数 | 位置 | 必填 | 说明 |
|---|---|---|---|
| `from_kind` | path | ✅ | `trace` / `metric` / `log` / `host` |
| `to_kind` | path | ✅ | `trace` / `metric` / `log` / `host` |
| `ctx` | query | ✅ | 源信号上下文的 base64-url JSON |

## 命令面板搜索

```http
GET /api/v1/web/search
```

跨实体 ⌘K 搜索：streams、dashboards、saved views、alerts、incidents、services。

| 参数 | 位置 | 必填 | 说明 |
|---|---|---|---|
| `q` | query | ✅ | 搜索文本 |
| `types` | query | — | `stream,dashboard,saved_view,alert,incident,service` 的逗号分隔子集 |
| `limit` | query | — | 默认 20，最大 50 |

返回至多 `limit` 条，按 `pg_trgm` 相似度排序。

## 服务拓扑

```http
GET /api/v1/web/topology          # 参数：from, to（RFC3339）
GET /api/v1/traces/service_graph  # 参数：from, to（微秒）, service —— edges[] + count
```

把 `service_graph_edges` 聚合为时间窗口内的节点与边。

## 追踪树

```http
GET /api/v1/web/trace/{trace_id}
```

某个 `trace_id` 的完整 span 树（≤ 100k span，32 MiB 硬上限 → `truncated=true`）。

## 调查栈

```http
POST /api/v1/web/investigation/blob        # 存储；返回 blob_id，7 天保留
GET  /api/v1/web/investigation/blob/{id}   # 按 blob_id 拉取
```
