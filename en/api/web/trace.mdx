---
title: "Trace tree"
description: "Full span tree for a trace_id."
---

```http
GET /api/v1/web/trace/{trace_id}
```

Full span tree for a `trace_id` (≤ 100k spans, 32 MiB hard cap → `truncated=true`).
