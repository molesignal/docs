---
title: "Edit schema"
description: "Adjust a stream's field schema."
---

```http
PUT /api/v1/streams/{id}/schema
```

Adjust field types or add fields. The schema also evolves automatically as new fields arrive.
