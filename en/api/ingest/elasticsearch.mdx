---
title: "Elasticsearch _bulk"
description: "Drop-in Elasticsearch _bulk ingest."
---

```http
POST /api/v1/_bulk
```

Elasticsearch `_bulk`-compatible ingest (NDJSON). Works with Filebeat, Logstash, and the Vector ES
sink. Returns a per-record status array (ES-compatible `items[]`).
