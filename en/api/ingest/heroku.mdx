---
title: "Heroku log drain"
description: "Heroku log drain destination."
---

```http
POST /api/v1/_heroku
```

Heroku log drain push (text). Returns `204` on success.

<Note>
  Authenticates with an `X-Connector-Token` header instead of a bearer token.
</Note>
