---
title: "Create function"
description: "Create a reusable pipeline transform."
---

```http
POST /api/v1/functions
```

Create a reusable transform. VRL is compiled on create; JavaScript requires the opt-in JS runtime.
See [Ingesting data → Pipeline functions](/en/ingestion#pipeline-functions).
