---
title: "Cloudflare Logpush"
description: "Cloudflare Logpush destination."
---

```http
POST /api/v1/_cloudflare
```

Cloudflare Logpush push (NDJSON). Returns `204` on success.

<Note>
  Authenticates with an `X-Connector-Token` header instead of a bearer token.
</Note>
