---
title: "Scheduled pipelines"
description: "List and create scheduled pipelines."
---

```http
GET  /api/v1/scheduled_pipelines
POST /api/v1/scheduled_pipelines
```

A scheduled pipeline runs on a cron expression (`every:Ns/m/h`). Creating one requires the
`DataPlaneAdmin` role.
