---
title: "Run saved view"
description: "Execute a saved query by id."
---

```http
POST /api/v1/saved_views/{id}/run
```

Run the saved query and return its result, the same shape as
[Execute a query](/en/api/query/execute).
